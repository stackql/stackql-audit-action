id: gcp-firewall-rdp-open
name: RDP (3389/tcp) open to the internet
severity: HIGH
description: |
  Ingress firewall rules that allow tcp/3389 from 0.0.0.0/0. Public RDP is a
  well-known ransomware entry point.
remediation: |
  Restrict sourceRanges to known IPs, or replace with IAP TCP forwarding.
query: |
  SELECT name, network, direction, priority, sourceRanges, allowed
  FROM google.compute.firewalls
  WHERE project = '${PROJECT_ID}'
    AND direction = 'INGRESS'
    AND disabled = false
filter: firewall_allows_port
filter_args:
  port: 3389
  protocol: tcp
columns: [name, network, priority]
